# snake_game-ver-1.0
python file(*.py)
You need to install the pygame library. If it is not installed yet, you can install it using the following command : pip install pygame
1.Make sure you have installed the pygame library.
2. Save the above code as snake_game.py.
3. Run this file in the terminal or command prompt : python snake_game.py
